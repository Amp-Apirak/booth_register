# Copy to .env (never commit .env)
DOMAIN=event-bbk.com
DB_USER=event_admin
DB_PASSWORD=change_me            # openssl rand -hex 24
JWT_SECRET=change_me             # openssl rand -hex 32
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
