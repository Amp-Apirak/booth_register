# QA — การทดสอบและบันทึกผล

ปรับปรุง: 2026-09-26

## 1. คำสั่งทดสอบ

| ชุด | คำสั่ง | ต้องมี | สถานะล่าสุด |
|---|---|---|---|
| Client typecheck | `cd client && npx tsc --noEmit` | — | ✅ ผ่าน |
| Client unit test | `cd client && TZ=Asia/Bangkok node --test tests/*.test.mjs` | Node 22+ (อ่าน `.ts` ได้เอง) | ✅ 24/24 |
| End-to-end (เบราว์เซอร์จริง) | `cd client && npx playwright test` | API + เว็บเปิดอยู่, Google Chrome, `.env.e2e.local` ([client/e2e/README.md](../client/e2e/README.md)) | ✅ 39/39 |
| Client production build | `cd client && npx next build` | — | ✅ (ทุก route เป็น dynamic เพราะอ่าน cookie ภาษา/ธีม) |
| Client lint | `cd client && npx eslint` | — | ⚠️ มี error เดิมก่อนงานรอบนี้ ไม่อยู่ใน CI · รอบ 2026-09-25 ไม่เพิ่ม error ใหม่ (dashboard ลดจาก 11 เหลือ 7) |
| Server integration | `cd server && npm test` (ทีละไฟล์ `--runInBand`) | PostgreSQL เปิดอยู่ + `server/.env` | ✅ 111/111 |
| Server syntax | `cd server && find . -name '*.js' -not -path './node_modules/*' -exec node --check {} \;` | — | ✅ |
| CI | GitHub Actions `.github/workflows/ci.yml` (push/PR เข้า `main`, `Dev`) | — | ควรเขียว (server `--runInBand`, client unit ครบทุกไฟล์) |

Unit test ฝั่ง client: `agenda.test.mjs` (สถานะกำหนดการ), `participantImport.test.mjs` (แปลงคอลัมน์รายชื่อ รวมประเภทองค์กร), `prizeImport.test.mjs` (แปลงคอลัมน์ของรางวัล), `analytics.test.mjs` (ตัวกรอง, KPI, จัดกลุ่มประเภทองค์กร, รายวัน/รายชั่วโมง, funnel, ตัวกรองใน URL — รันด้วย `TZ=Asia/Bangkok`)

Server: `organizationTypes.test.js` (list สาธารณะ, ต้อง token, สีนอกชุด → 400, บังคับเลือกตอนลงทะเบียน, "อื่นๆ", ลบที่มีคนใช้ → 409, ประเภทที่ปิดแล้วลงทะเบียนไม่ได้)

## 2. บันทึกการตรวจรับ (2026-09-23, branch `Dev`)

| ฟีเจอร์ | ตรวจอะไรแล้ว | วิธี | ยังไม่ได้ตรวจ |
|---|---|---|---|
| สแกน QR ด้วยกล้อง `/scanner` | typecheck, lint, หน้า render | อัตโนมัติ | สแกนตั๋วจริงหน้ากล้อง |
| สถิติ REST = WebSocket | `/stats` ตอบรูปแบบใหม่; จอ Overview ไม่พังเมื่อเช็คอิน | API + ผู้ใช้ทดสอบ | — |
| จอ LED ไม่เปลี่ยนหน้าเอง | ทุก `?screen=` render; ผู้ใช้ยืนยันปัญหาเดิม | curl | สแกนจริงขณะเปิดหลายจอ |
| หน้าประกาศผู้โชคดี | ภาพหน้าจอ 1512×945, 1920×1080 ไม่ล้นกรอบ | headless Chrome | โหมดเต็มจอจริง, ผู้ชนะที่มีรูป, รางวัลที่มีรูป |
| นำเข้ารายชื่อ | 400 แถว, ข้ามอีเมลซ้ำ/ขาดชื่อ/อีเมลผิด, นำเข้าซ้ำไม่ซ้ำ, รหัสตั๋วไม่ชน, 401 ไม่มี token | supertest กับ DB จริง + ผู้ใช้นำเข้า 20 แถวสำเร็จ | — |
| กำหนดการ: ไฟล์ตัวอย่าง/เลขลำดับ/การ์ดใหม่บนสุด | typecheck, lint | อัตโนมัติ | คลิกใช้งานจริง |
| แท็บตั้งค่าใน URL | ทุก `?tab=` render | curl | — |
| ของรางวัล: ลำดับ/นำเข้า/ส่งออก | reorder, import อัปเดต+สร้าง, รูปเดิมคงอยู่, แจ้งเลขแถวผิด, 401 | supertest กับ DB จริง (คืนข้อมูลแล้ว) | หน้าจอจริง (ต้อง login) |
| Footer + `/privacy` | 3 แบบตามหน้า, ชิดล่างจอ, ลิงก์แผนที่ไม่ใช่ http → 400 | headless Chrome + API | กรอกฟอร์มตั้งค่าจริง |
| ติดตั้งใหม่ | schema+seed บน DB ว่าง, คอลัมน์ตรงกับ DB ใช้งาน, server init ผ่าน, `setup.sh` (sandbox), `create-admin.js` + login | สคริปต์ | รัน `setup.sh` บนเครื่องใหม่จริง |

### รอบ 2026-09-25 (ภาษา/ธีม, ประเภทองค์กร, รายงานและกราฟ)

| ฟีเจอร์ | ตรวจอะไรแล้ว | วิธี | ยังไม่ได้ตรวจ |
|---|---|---|---|
| 2 ภาษา + 2 ธีม | key ครบทั้ง 2 ภาษา (typecheck); หัวหน้าลงทะเบียนบนมือถือ 390px ไม่ทับฟอร์ม; ภาพหน้าจอ register, dashboard, settings, รายงานและกราฟ, login ในหลายชุด TH/EN × มืด/สว่าง; เมนูบนอยู่แถวเดียวที่ 1280/1440/1536/1920 px (เจ้าหน้าที่ + บุคคลทั่วไป), 1024 px ใช้ปุ่มเมนู; ข้อความ error login แปลตามภาษา | headless Chrome | ไล่ทุกหน้าทั้ง 4 แบบด้วยตาบนจอจริง โดยเฉพาะ `/signage`, `/lucky-draw`, `/scanner`, `/ticket` |
| ประเภทองค์กร | 8 เทสต์ API กับ DB จริง; migration บน DB ที่มีข้อมูลอยู่ (restart แล้วได้ 6 ประเภท, ผู้เข้าร่วมเดิม = ไม่ระบุ); รายการเลือก (dropdown) ในหน้า register ทั้งเดสก์ท็อปและมือถือ 390px + ข้อความเตือนเมื่อไม่เลือก; แท็บตั้งค่า; คอลัมน์ + ตัวกรอง + เรียงใน Dashboard | Jest + headless Chrome | กรอกฟอร์ม `/register` จริงบนมือถือ, นำเข้า Excel ที่มีคอลัมน์ประเภทองค์กร |
| ตาราง Dashboard | ความกว้างพอดีที่ 1440 และ 1920 px (ไม่มีแถบเลื่อน), 1280 px คอลัมน์ "จัดการ" ติดขอบขวา | วัดขนาดด้วย DevTools protocol | จอแท็บเล็ต |
| รายงานและกราฟ | 9 unit test ของ `analytics.ts`; ภาพหน้าจอ 1440/1920 px ทั้ง 2 ธีม | อัตโนมัติ + headless Chrome | คลิกกรองไขว้ทุกกราฟด้วยมือ, ข้อมูลหลายพันแถว |
| บันทึกรูป QR / ตั๋ว | แดชบอร์ด: ชื่อยาว + ไม่มีวันเวลางาน (กรณีที่เคยเยื้อง) ได้รูปกึ่งกลาง; หน้าลงทะเบียน + หน้าตั๋วในโทนสว่างอ่านได้ | ดาวน์โหลดไฟล์จริงผ่าน headless Chrome (ลงทะเบียนทดสอบ 2 รายการแล้วลบ) | Safari / มือถือจริง |
| รายงาน A4 | PDF ไทย/อังกฤษ 3 หน้า, ขอบ 2.5/2/3/2 ซม., เลขหน้า, หัวตารางซ้ำเมื่อขึ้นหน้าใหม่, ตารางสั้นไม่แยกหน้า, สีพิมพ์ออกแม้ปิด "Background graphics" | Chrome `printToPDF` | เครื่องพิมพ์จริง, Safari/Firefox |

### รอบ 2026-09-26 (รองรับทุกขนาดจอ)

| ตรวจอะไร | วิธี | ผล | ยังไม่ได้ตรวจ |
|---|---|---|---|
| ไม่มีส่วนใดล้นขอบจอ: 22 หน้า/สถานะ (บุคคลทั่วไป + เจ้าหน้าที่, จอ LED 4 หน้า, ตั้งค่า 5 แท็บ, หน้าต่างเพิ่ม/นำเข้า, เมนูมือถือ, ตั๋วที่ค้นเจอ) × 6 ความกว้าง 360 / 390 / 768 / 1024 / 1440 / 1920 px | สคริปต์ headless Chrome วัดกรอบทุก element เทียบความกว้างจอ (ข้าม element ที่อยู่ในกล่องเลื่อน/ซ่อนขอบ) | ✅ 132/132 | — |
| ภาพหน้าจอทุกหน้าที่ 390 (มือถือ) และ 768 px (แท็บเล็ต) + ตัวอย่าง 1440 px ว่าเดสก์ท็อปไม่เปลี่ยน | ตรวจด้วยตา ทั้งโทนมืด/สว่าง ไทย/อังกฤษ | ✅ | iPhone/Android เครื่องจริง |
| ปุ่มเต็มจอบนเครื่องที่ไม่รองรับ (จำลอง `fullscreenEnabled = false`) → หน้าจอคลุมทั้งหน้า, ปุ่มออก | headless Chrome | ✅ | Safari บน iPhone/iPad จริง |
| สั่งพิมพ์รายงานจากหน้าจอ 390 px → ได้ A4 ขนาดจริง (ไม่ถูกย่อ) | Chrome `printToPDF` | ✅ | |

### รอบ 2026-09-26 (ข้อมูลสดจอ LED, ความปลอดภัย, สิทธิ์, ทดสอบอัตโนมัติ)

| ตรวจอะไร | วิธี | ผล |
|---|---|---|
| จอ LED อัปเดตทันทีเมื่อกด **สแกนเข้า** (แดชบอร์ด) หรือสแกนที่หน้า `/scanner`: หน้าต้อนรับแบบย่อ, แบบเต็มจอ, ตัวเลขภาพรวมแบบเต็มจอ, เต็มจอแบบ iPhone, จอ LED ที่เปิดจากเครื่องอื่นผ่าน IP (บล็อก localhost ไว้), ป้าย "ขาดการเชื่อมต่อ" และกลับมาเอง, สแกนซ้ำบอกชื่อ/เวลา | Playwright `e2e/led-realtime.spec.ts` (8) | ✅ |
| สแกน QR ผ่าน**กล้อง**จริง (กล้องจำลองเล่นวิดีโอ QR ของตั๋ว) → เช็คอิน → จอ LED แสดงทันที; พิมพ์/เครื่องยิง: สำเร็จ, ซ้ำ, ไม่พบ | `e2e/scanner.spec.ts` (2) | ✅ |
| สุ่มรางวัล: หน้าควบคุม + จอ LED แสดงผู้ชนะทันที, แจกไม่เกินจำนวน, ต้อง login | `e2e/lucky-draw.spec.ts` (3) | ✅ |
| login ผิด/ถูก, หน้าเจ้าหน้าที่ 5 หน้าเมื่อยังไม่ login → login → กลับหน้าเดิม, Staff ไม่เห็นตั้งค่า/ลบ/นำเข้า (server ตอบ 403), Admin ทำได้, ออกจากระบบ | `e2e/auth-roles.spec.ts` (9) | ✅ |
| แดชบอร์ด: เพิ่ม (ประเภทองค์กร) → แก้ไข (เบอร์โทร) → เปลี่ยนประเภทในแถว → สแกนเข้า → บันทึกรูป QR → ส่งออก Excel → ลบ; แท็บกราฟกรองได้ → รายงาน A4 ตามตัวกรอง | `e2e/dashboard.spec.ts` (2) | ✅ |
| หน้าแรก, ลงทะเบียน (ต้องเลือกประเภทองค์กร) → ตั๋ว → บันทึกรูป → ค้นตั๋วจากอีกเครื่องด้วยรหัส + เบอร์ 4 ตัวท้าย (ผิด/ถูก), สลับภาษา/ธีมแล้วจำค่า, นโยบายความเป็นส่วนตัว, แท็บจอ LED อยู่ใน URL | `e2e/public.spec.ts` (5) | ✅ |
| ตั้งค่า 5 แท็บ + รีเฟรชแล้วอยู่แท็บเดิม, บันทึก, ดาวน์โหลดไฟล์ตัวอย่าง, เพิ่มประเภทองค์กรแล้วเห็นในหน้าลงทะเบียน แล้วลบ | `e2e/settings.spec.ts` (4) | ✅ |
| หน้าสาธารณะ 9 + หน้าเจ้าหน้าที่ 10 ไม่ล้นจอที่ 360 / 390 / 768 px | `e2e/responsive.spec.ts` (6) | ✅ |
| API: ทุก endpoint ของเจ้าหน้าที่ 401 เมื่อไม่ login, Admin-only 403 สำหรับ Staff, token ปลอม/หมดอายุ, ตรวจข้อมูลลงทะเบียน (อีเมล เบอร์ ความยาว รูป VIP), ค้นหาตั๋ว + พักเมื่อผิด 5 ครั้ง, login ผิด 10 ครั้ง, สแกนตั๋วเดียวกันพร้อมกัน 2 จุด, สุ่มพร้อมกัน 2 ครั้ง, รางวัลปิด/ไม่มี | Jest `security.test.js` (71) + `api.test.js` (19) | ✅ |
| ข้อมูลสด: จอที่ไม่ login ไม่ได้รายชื่อ/อีเมล/เบอร์, หน้าจอเจ้าหน้าที่ได้, token ปลอมไม่ได้, ต้อนรับส่งก่อนรายชื่อ, ตัวเลขตรงกับ API | `realtime.test.js` (5) | ✅ |
| อีเมลตั๋วใช้ข้อมูลงานจริง, escape HTML, เวลาไทยไม่เพี้ยนตาม timezone | `email.test.js` (4) | ✅ |
| ชุดรวม | Playwright 39/39 · Jest 111/111 · client unit 24/24 · typecheck · `next build` · ไม่มี lint error ใหม่ | ✅ ข้อมูลทดสอบถูกลบหมด (DB กลับเป็น 48 คน / เช็คอิน 30 / ผู้ได้รางวัล 10) |

**License:** ระบบนี้ไม่มีระบบ License/สิทธิ์การใช้งานแบบแพ็กเกจ จึงไม่มีสิ่งให้ทดสอบในหัวข้อนี้

## 3. ปัญหาที่ทราบ (Known issues)

| # | ปัญหา | ผลกระทบ | แนวทาง |
|---|---|---|---|
| ~~Q-1~~ | ~~เทสต์ server 9 ข้อคาดพฤติกรรมเก่า~~ | แก้แล้ว 2026-09-26: เขียนใหม่ตามพฤติกรรมปัจจุบัน + ลบข้อมูลที่สร้างเอง (เดิมเทสต์สุ่มรางวัลสร้าง "ผู้ชนะ iPad Pro 2026" ค้างใน DB ทุกครั้ง — ลบ 8 รายการในเครื่องพัฒนาแล้ว สำรองไว้) · server 111/111 | |
| ~~Q-2~~ | ~~`POST /lucky-draw/spin` ไม่ต้อง login~~ | แก้แล้ว 2026-09-26 ([ADR-0015](adr/0015-roles-and-access.md)) | |
| Q-3 | กล้องสแกนใช้ไม่ได้บน `http://<IP>` | แท็บเล็ตบน LAN สแกนด้วยกล้องไม่ได้ | ใช้ HTTPS หรือเครื่องยิงบาร์โค้ด |
| ~~Q-4~~ | ~~รหัสตั๋วมี ~9,000 ค่า/วัน~~ | แก้แล้ว 2026-09-26: สุ่ม 6 หลัก + สุ่มใหม่เมื่อชน (เดิมลงทะเบียนล้มเหลวได้เมื่อรหัสชน) | |
| Q-5 | ESLint error เดิมหลายไฟล์ (dashboard, settings, signage, register, Navigation) | ไม่กระทบการทำงาน | เก็บกวาดแล้วเพิ่ม lint ใน CI |
| ~~Q-6~~ | ~~ปุ่มลอย "พักพื้นหลัง" ทับข้อความใน Footer~~ | แก้แล้ว 2026-09-25 (ระยะขวาของ Footer หายที่จอ ≥1024px เพราะ `lg:px-8` ทับ `sm:pr-48`) | |
| Q-7 | ลิงก์ LINE ของผู้ติดต่อแพลตฟอร์มใช้ `line.me/ti/p/~0619522111` | ใช้ได้เฉพาะถ้า LINE ID เป็นค่านี้จริง | ยืนยัน LINE ID / ลิงก์เพิ่มเพื่อน |
| Q-8 | Redis อยู่ใน docker-compose แต่โค้ดไม่ใช้ | ใช้ทรัพยากรเปล่า | ใช้งานหรือถอดออก |
| Q-9 | โหมดภาษาอังกฤษ: รายละเอียด error จาก server ในหน้าต่างแจ้งเตือนของเจ้าหน้าที่บางจุดยังเป็นภาษาไทย (ตั้งค่าทั่วไป, กำหนดการ, ของรางวัล, หน้าลงทะเบียน, นำเข้ารายชื่อ) หัวข้อแจ้งเตือนแปลแล้ว | เห็นเฉพาะตอนบันทึกไม่ผ่าน | แปลตามรหัส `error` แบบหน้า login / ประเภทองค์กร |
| Q-10 | เลขหน้าในรายงาน A4 ใช้ CSS `@page` margin box | เบราว์เซอร์ที่ไม่รองรับจะไม่มีเลขหน้า | พิมพ์จาก Chrome/Edge |
| Q-11 | กราฟคำนวณในเบราว์เซอร์จากรายชื่อทั้งหมด | งานหลายหมื่นคนอาจช้า | ย้ายไปรวมใน SQL (ADR-0014) |
| ~~Q-13~~ | ~~เปิด `/scanner` โดยไม่ login แล้วสแกนถูกพาไปหน้า login~~ | แก้แล้ว 2026-09-26 (ตัวเลือก A): หน้าเจ้าหน้าที่แสดงปุ่มเข้าสู่ระบบแล้วกลับมาหน้าเดิม | |
| Q-15 | อีเมลตั๋วโหลดรูป QR จากบริการภายนอก (api.qrserver.com) และส่งรหัสตั๋วไปด้วย | อีเมลบางโปรแกรมบล็อกรูปภายนอก; รหัสตั๋วผ่านบุคคลที่สาม | สร้างรูป QR ใน server แล้วแนบในอีเมล (ต้องเพิ่มไลบรารี QR ฝั่ง server) |
| Q-16 | ตัวนับ login ผิด / ค้นหาตั๋วผิด อยู่ในหน่วยความจำของ server | restart แล้วนับใหม่; ถ้าอนาคตรันหลาย instance ต้องย้ายไปเก็บร่วม (Redis) | ปัจจุบันรัน 1 instance (Socket.io ก็ต้องการแบบนี้) |
| Q-14 | iPhone ไม่มีโหมดเต็มจอจริงสำหรับหน้าเว็บ ระบบจึงขยายหน้าจอคลุมหน้าเบราว์เซอร์แทน (แถบที่อยู่ของ Safari ยังเห็น) | จอ LED ที่ใช้ iPhone ไม่เต็มจอ 100% | ใช้คอมพิวเตอร์/แท็บเล็ต/TV browser เป็นเครื่องเปิดจอ LED |
| ~~Q-12~~ | ~~หน้า `/ticket` ใช้ API ของเจ้าหน้าที่~~ | แก้แล้ว 2026-09-26: `POST /tickets/lookup` (รหัสตั๋ว + เบอร์ 4 ตัวท้าย/อีเมล, จำกัดการลองผิด) | |

## 4. เช็กลิสต์ทดสอบด้วยมือ (ก่อนรวมเข้า `main`)

- [ ] ลงทะเบียนที่ `/register` (มี/ไม่มีรูป) → ได้ตั๋ว QR, ลิงก์นโยบายเปิดได้
- [ ] `/scanner` เปิดกล้อง → สแกนตั๋ว → เสียง beep + แถบเขียว; สแกนซ้ำ → แจ้งเตือน
- [ ] เปิด `/signage?screen=overview` และ `?screen=welcome` พร้อมกัน → เช็คอิน → Overview ตัวเลขเปลี่ยน ไม่เปลี่ยนหน้า / Welcome เด้งชื่อ+พลุ
- [ ] `/lucky-draw` สุ่ม → หน้านั้นแสดงชื่อ; `/signage?screen=lucky` แสดงผู้โชคดีพร้อมรูป ไม่ล้นจอ (ทดสอบโหมดเต็มจอ)
- [ ] `/dashboard` นำเข้า Excel ไฟล์ตัวอย่าง + ไฟล์ส่งออก → จำนวนถูกต้อง นำเข้าซ้ำไม่เพิ่ม
- [ ] `/settings?tab=agenda` ไฟล์ตัวอย่าง → นำเข้า → บันทึก → จอ Agenda
- [ ] `/settings?tab=prizes` เพิ่ม/เลื่อนลำดับ/นำเข้า/ส่งออก
- [ ] `/settings` กรอกข้อมูลติดต่อ → Footer หน้าแรก + `/privacy` แสดงถูกต้อง; จอ LED ไม่มี Footer
- [ ] รีเฟรชทุกหน้าที่มี `?screen=` / `?tab=` → อยู่หน้าเดิม
- [ ] สลับ TH/EN และมืด/สว่าง ทุกหน้า (รวมจอ LED, Lucky Draw, Scanner, ตั๋ว) → ไม่มีข้อความตกค้าง/สีอ่านไม่ออก; รีเฟรชแล้วคงค่า; `?lang=en&theme=light` บนจอ LED
- [ ] `/register` ไม่เลือกประเภทองค์กร → แจ้งเตือนและเลื่อนไปที่ช่อง; เลือก "อื่นๆ" ต้องพิมพ์ชื่อ → ลงทะเบียนแล้วเห็นในคอลัมน์ Dashboard
- [ ] `/settings?tab=organizations` เพิ่ม/แก้/เลื่อน/ซ่อน/ลบ → `/register` เปลี่ยนตามทันที; ลบประเภทที่มีคนเลือก → แจ้งให้ซ่อนแทน
- [ ] `/dashboard?tab=analytics` คลิกกราฟแต่ละแบบ → กราฟอื่นกรองตาม; ตัวกรองวันเวลา → ตัวเลขตรงกับตารางรายชื่อที่กรองเหมือนกัน
- [ ] พิมพ์รายงาน A4 ด้วยเครื่องพิมพ์จริง (Scale 100%, ปิด Headers and footers) → ขอบ, เลขหน้า, สี, ช่องลงนามถูกต้อง
- [ ] เปิดทุกหน้าบนมือถือจริง (iPhone + Android) และแท็บเล็ต ทั้งแนวตั้ง/แนวนอน → ไม่มีส่วนล้นจอ, ปุ่มกดง่าย; จอ LED กด "เต็มจอ" แล้วออกได้
