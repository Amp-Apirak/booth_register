# Changelog

รูปแบบ: หัวข้อตามวันที่ (ค.ศ.) ใหม่อยู่บน · อ้างอิง commit บน branch `Dev` · เหตุผลเชิงออกแบบอยู่ใน [docs/decision.md](docs/decision.md)

## 2026-09-26 (รอบ 3) — สำรองข้อมูลและรีเซ็ตระบบเมื่อจบงาน

### เพิ่ม
- **ทุกแท็บของหน้าตั้งค่ามีปุ่ม "ส่งออก Excel" และ "รีเซ็ตเป็นค่าเริ่มต้น"** (ข้อมูลทั่วไป, หน้าลงทะเบียน, ประเภทองค์กร, กำหนดการ, ของรางวัล) · ปุ่ม "คืนค่าเริ่มต้น" เดิมของข้อมูลทั่วไปล้างแค่ฟอร์มและไม่มีหน้าต่างยืนยัน ตอนนี้รีเซ็ตจริงหลังยืนยัน ([ADR-0017](docs/adr/0017-backup-and-reset.md)) (`2b2f933`)
- **แท็บใหม่ "สำรองและรีเซ็ต"** `/settings?tab=backup` (Admin): จำนวนข้อมูลแต่ละหมวด, ส่งออก/รีเซ็ตทีละหมวด (รวม "ข้อมูลผู้เข้าร่วม": รายชื่อ เช็คอิน ผู้ได้รางวัล), **ส่งออกทั้งหมด** เป็นไฟล์เดียว (`event-backup-วันที่-เวลา.xlsx` ชีตสรุป + 7 ชีต) และ **รีเซ็ตทั้งระบบ (เริ่มงานใหม่)** (`2b2f933`)
- **หน้าต่างยืนยันทุกปุ่มรีเซ็ต:** บอกสิ่งที่จะถูกลบพร้อมจำนวนจริง, ปุ่ม "ส่งออก Excel ก่อน" (ดาวน์โหลดแล้วหน้าต่างยังเปิดอยู่), บัญชีผู้ใช้ไม่ถูกลบ, กู้คืนไม่ได้ · ลบรายชื่อผู้เข้าร่วมหรือรีเซ็ตทั้งระบบต้องพิมพ์ **RESET** (`2b2f933`)
- API `GET /events/:id/reset-summary`, `POST /events/:id/reset` (Admin, ทั้งหมดหรือไม่มีเลยใน transaction เดียว) · WebSocket `data:reset` · จอ LED ที่เปิดอยู่เปลี่ยนตามทันที (ชื่องาน, ตัวเลขเป็น 0, ล้างชื่อแขกคนล่าสุดและผู้ได้รางวัล) หน้าสุ่มรางวัลโหลดใหม่ (`937b90f`, `2b2f933`)

### เปลี่ยน
- Excel ผู้เข้าร่วม (แดชบอร์ด) เพิ่มคอลัมน์ "วันเวลาที่เช็คอิน" และมีหัวคอลัมน์แม้ไม่มีข้อมูล · ตัวสร้าง Excel ของทุกหมวดรวมไว้ที่ `client/src/lib/backupExport.ts` (`2b2f933`)
- ค่าเริ่มต้นของการตั้งค่าอยู่ที่เดียว (`DEFAULT_SETTINGS` ใน `settingsRepository.js`) แทนการเขียนซ้ำ 3 ที่ (`937b90f`)
- แถบแท็บตั้งค่า (6 แท็บ): มือถือ 2 คอลัมน์ · แท็บเล็ต 3 คอลัมน์ · ตั้งแต่ 1024 px แถวเดียว · ปุ่มรีเซ็ตบนมือถือแสดงคำว่า "รีเซ็ต" (`2b2f933`)
- api_spec: endpoint ที่เป็น Admin เท่านั้น (กำหนดการ ของรางวัล ประเภทองค์กร ตั้งค่า นำเข้า) เดิมเขียนว่า Staff JWT แก้เป็น Admin JWT

### ทดสอบ
- Playwright `e2e/backup-reset.spec.ts` (7): ส่งออกทุกแท็บและไฟล์รวม (เปิดไฟล์ตรวจเนื้อหา), หน้าต่างยืนยัน/ยกเลิก/พิมพ์ RESET, รีเซ็ตจริงทีละหมวดและทั้งระบบพร้อมตรวจจอ LED, Staff ถูกปฏิเสธ, จอ 360 px · ข้อมูลในเครื่องถูกคัดลอกไว้ก่อนและคืนครบ (เทียบกับไฟล์สำรอง `pg_dump` ตรงกันทุกตาราง) (`c67b17d`)
- Jest `reset.test.js` (10) + สิทธิ์ endpoint ใหม่ใน `security.test.js` · รวม Playwright 46/46 · Jest 127/127 · unit 24/24 (`881ef3f`)

## 2026-09-26 (รอบ 2) — จอ LED แบบเรียลไทม์, ความปลอดภัย, สิทธิ์ และทดสอบอัตโนมัติ

### แก้ไข
- **จอ LED ไม่อัปเดตเมื่อเปิดจากเครื่องอื่น (มือถือ/ทีวี/PC จอ LED ผ่าน IP):** โหมดพัฒนาของ Next.js ปฏิเสธไฟล์ JavaScript (403) และหน้าเว็บเรียก API ที่ `localhost` ของเครื่องตัวเอง → อนุญาต IP ภายใน (`allowedDevOrigins`) และเรียก API ที่ IP เดียวกับหน้าเว็บอัตโนมัติ (ADR-0016) (`b9f67d9`)
- สแกนตั๋วซ้ำบอก "ชื่อ เช็คอินไปแล้วเมื่อ HH:MM" แทน "ไม่พบรหัสตั๋ว" · ปุ่ม **สแกนเข้า** ในแดชบอร์ดแจ้งเมื่อไม่สำเร็จ (เดิมเงียบ) · สองจุดสแกนตั๋วเดียวกันพร้อมกันได้ "เช็คอินไปแล้ว" แทน error (`03e45d6`, `b9f67d9`)
- **อีเมลตั๋วเคยเป็นงานตัวอย่าง** ("Tech Innovation Summit 2026", 30 ส.ค., Central Plaza) → ใช้ชื่องาน วันเวลา สถานที่ ผู้ติดต่อจากหน้าตั้งค่า และ escape ข้อความจากผู้ลงทะเบียน · หน้าลงทะเบียน/หน้าตั๋วไม่แสดงวันที่และสถานที่ตัวอย่างอีกต่อไป (`31c8e18`, `b9f67d9`)
- **ลงทะเบียนล้มเหลวเมื่อรหัสตั๋วชนกัน** (4 หลัก ~40% ที่ 100 คน/วัน) → สุ่ม 6 หลัก + สุ่มใหม่เมื่อชน (`03e45d6`)
- **แจกรางวัลเกินจำนวนได้** → server ตรวจรางวัลมีอยู่ เปิดสุ่ม เหลือจำนวน และสุ่มทีละครั้ง · หน้าสุ่มรางวัลแจ้งเหตุผลเมื่อสุ่มไม่ได้ และแสดงผู้ได้รางวัลเดิมเมื่อเปิดหน้าใหม่ (`03e45d6`, `b9f67d9`)
- หน้าต่างแก้ไขผู้เข้าร่วมแก้อีเมล/เบอร์โทรได้ (เดิมไม่มีช่อง) (`b9f67d9`)
- ตัวเลขสรุปนับด้วย SQL แทนการโหลดรายชื่อทั้งหมดทุกครั้งที่มีคนเช็คอิน (`03e45d6`)

### ความปลอดภัย
- **ข้อมูลส่วนบุคคลรั่ว:** ทุกครั้งที่ลงทะเบียน/เช็คอิน server ส่งรายชื่อพร้อมอีเมล เบอร์โทร และรูป ไปทุกหน้าจอรวมผู้ที่ไม่ได้ login → ส่งเฉพาะหน้าจอเจ้าหน้าที่ที่ยืนยัน token (ADR-0016) · ผลสุ่มรางวัลไม่มีอีเมล (`03e45d6`, `b9f67d9`)
- **สิทธิ์ 3 ระดับ** สาธารณะ / เจ้าหน้าที่ / Admin (ADR-0015): สุ่มรางวัลและรายชื่อผู้มีสิทธิ์ต้อง login · Staff ตั้งค่า ลบ นำเข้าไม่ได้ (server ตอบ 403, หน้าเว็บซ่อนปุ่ม/เมนู) (`03e45d6`, `b9f67d9`)
- ค้นหาตั๋วสาธารณะ `POST /tickets/lookup` (รหัสตั๋ว + เบอร์ 4 ตัวท้าย/อีเมล) แทนการใช้ API เจ้าหน้าที่ · จำกัดการลองผิด (ค้นหาตั๋ว 5 ครั้ง/รหัส, login 10 ครั้ง) · `TRUST_PROXY` ใน deploy (`03e45d6`, `b9f67d9`)
- ลงทะเบียนสาธารณะตรวจความยาว อีเมล เบอร์ รูป และบังคับ General (เดิมส่ง `attendee_type: "VIP"` เองได้) (`03e45d6`)
- บันทึกผู้สแกนใน `checkins.scanned_by` (`03e45d6`)

### เปลี่ยน
- หน้าเจ้าหน้าที่ (สแกน, แดชบอร์ด, รายงาน, สุ่มรางวัล, ตั้งค่า) เมื่อยังไม่ login แสดงปุ่มเข้าสู่ระบบแล้วกลับมาหน้าเดิม · เมนู Lucky Draw เห็นเฉพาะเจ้าหน้าที่, ตั้งค่าระบบเฉพาะ Admin (`b9f67d9`)
- แท็บละ 1 การเชื่อมต่อสด (เดิม 3) สำรองด้วย long-polling · จอ LED เต็มจอแสดงป้าย "ขาดการเชื่อมต่อ" เมื่อหลุด (`b9f67d9`)
- `/ticket` สำหรับผู้เข้าร่วม: ช่องรหัสตั๋ว + เบอร์โทร 4 ตัวท้าย (`b9f67d9`)

### ทดสอบ
- **Playwright** (`client/e2e/`, 39 ชุด): จอ LED ทุกกรณี, สแกน QR ผ่านกล้องจริง (กล้องจำลอง), สุ่มรางวัล, login/สิทธิ์, แดชบอร์ด, ลงทะเบียน/ค้นหาตั๋ว, ตั้งค่า, ขนาดจอ (`535e896`)
- **Jest** 111 ชุด (เดิมผ่าน 18/27): เขียนเทสต์เดิมใหม่ + ความปลอดภัย/สิทธิ์ + ข้อมูลสด + อีเมล · ทุกชุดลบข้อมูลที่สร้างเอง (เดิมเทสต์สุ่มรางวัลทิ้ง "ผู้ชนะ iPad Pro 2026" ไว้ใน DB — ลบ 8 รายการในเครื่องพัฒนาแล้ว) (`2836997`)
- CI: server รันทีละไฟล์, client รัน unit test ครบทุกไฟล์ (เดิมรันไฟล์เดียว) (`2836997`)

## 2026-09-26 — รองรับทุกขนาดจอ (มือถือ · แท็บเล็ต · คอมพิวเตอร์ · จอ LED)

### เปลี่ยน
- **จอ LED `/signage` บนมือถือ/แท็บเล็ต:** หน้าจอยืดตามเนื้อหาแล้วเลื่อนดูได้ (เดิมถูกบีบให้สูงเท่าจอ เนื้อหาถูกตัด) · แถบเลือกหน้าเป็นตาราง 2×2 และแปลไทย/อังกฤษ · ขนาดตัวอักษร/รูปในโหมดเต็มจอปรับตามความกว้างจอ (`0c7ba32`, `b9f67d9`)
- **ปุ่มเต็มจอใช้ได้ทุกเครื่อง:** เบราว์เซอร์ที่ไม่รองรับเต็มจอ (iPhone) จะขยายหน้าจอคลุมทั้งหน้าเบราว์เซอร์แทน · มีปุ่ม "ออกจากเต็มจอ" มุมขวาบน (โผล่เมื่อแตะ/ขยับเมาส์ แล้วหายเองใน 3 วินาที ไม่บังภาพบนจอ LED) · รองรับ Safari บน iPad รุ่นเก่า (webkit) (`b9f67d9`)
- **แดชบอร์ด:** จอเล็กกว่า 1024px แสดงผู้เข้าร่วมเป็นการ์ด (แท็บเล็ตแถวละ 2 ใบ) พร้อมปุ่มสแกนเข้า/QR/แก้ไข/ลบ และเลือกประเภทองค์กรได้ · ตัวเลขสรุป 2×2 · ปุ่มส่งออก/นำเข้า/เพิ่มผู้ร่วมงาน และตัวกรองขึ้นบรรทัดใหม่แทนการล้นจอ · หน้าต่างเพิ่ม/แก้ไข/นำเข้าสูงพอดีจอมือถือ (`b9f67d9`)
- **รายงาน A4 บนจอเล็ก:** แสดงทั้งแผ่นแบบย่อ (เหมือนดู PDF) ไม่ล้นจอ · สั่งพิมพ์ยังเป็นขนาดจริง 100% · แถบปุ่มด้านบนใช้สีตามธีมของเว็บ (`0c7ba32`)
- **ตั้งค่าระบบ:** แท็บเป็นตาราง 2 คอลัมน์บนมือถือ · การ์ดกำหนดการเรียงรูปวิทยากรไว้บน ช่องกรอกเต็มความกว้าง (เดิมถูกบีบจนอ่านไม่ออก) · ปุ่มเปลี่ยนหน้าแบบย่อ · สีในกราฟของประเภทองค์กรขึ้นบรรทัดใหม่ได้ (`0c7ba32`, `b9f67d9`)
- **กราฟ:** ชื่อกราฟยาวขึ้นบรรทัดใหม่แทนการตัด "…" · แกนเวลาเช็คอินแสดงทุก 2–3 ชั่วโมงเมื่อจอแคบ (`0c7ba32`)

### แก้ไข
- ป้าย "CHECKED-IN" บนตั๋วถูกตัดเป็น 2 บรรทัดบนมือถือ (`b9f67d9`)
- ปุ่มลอย "พักพื้นหลัง" บังบรรทัดสุดท้ายของ Footer บนมือถือ (`0c7ba32`)
- หัวหน้าแรก (Event Experience) บนมือถือ: ปุ่มพักเอฟเฟกต์ถูกบีบเป็น 2 บรรทัด → เหลือไอคอน (`0c7ba32`)
- ตัวย่อชื่อผู้โชคดีบนจอ Lucky เป็นสีเข้มในโทนสว่าง → ขาวเสมอ (`0c7ba32`)

## 2026-09-25

### เพิ่ม
- **2 ภาษา (ไทย/อังกฤษ) + 2 ธีม (มืด/สว่าง) ทุกหน้า** — ปุ่ม TH/EN และ ☀/🌙 บนเมนู จำค่าไว้ในเครื่อง, URL `?lang=` / `?theme=` สำหรับจอ LED; ข้อความทั้งหมดอยู่ใน `client/src/i18n/` ([ADR-0013](docs/adr/0013-theme-and-language.md)) (`766c7b5`, `404b7e8`)
- **ประเภทองค์กร** — ช่องบังคับเลือกในหน้า `/register` (+ "อื่นๆ โปรดระบุ"), แท็บ `/settings?tab=organizations` (ชื่อไทย/อังกฤษ, สี 8 สีที่ตรวจแล้ว, ซ่อน/แสดง, ลำดับ), คอลัมน์ในแดชบอร์ดที่เจ้าหน้าที่เลือกแทนได้, ตัวกรอง/เรียง, คอลัมน์ในไฟล์นำเข้า/ส่งออก Excel; API `/events/:id/organization-types` ([ADR-0012](docs/adr/0012-organization-types.md)) (`743743b`, `ca12b58`, `c45a916`)
- **แท็บรายงานและกราฟ** `/dashboard?tab=analytics` — ค้นหา, ตัวกรองประเภทองค์กร/สถานะ/ประเภทผู้เข้าร่วม, ช่วงวันเวลา, ตัวเลขสรุป 6 ค่า, กราฟ 6 แบบที่คลิกแล้วกรองกันไขว้, ปุ่มดูตารางทุกกราฟ, อัปเดตสด ([ADR-0014](docs/adr/0014-analytics-and-a4-report.md)) (`c45a916`)
- **รายงาน A4 แนวตั้ง** `/dashboard/report` — ขอบแบบหนังสือราชการ (บน 2.5 ล่าง 2 ซ้าย 3 ขวา 2 ซม.), ฟอนต์ Sarabun, เลขหน้า, ช่องลงนาม, เฉพาะตัวเลขสรุป (ไม่มีข้อมูลรายบุคคล) (`c45a916`)
- เทสต์: `client/tests/analytics.test.mjs`, `server/tests/organizationTypes.test.js`; ADR 0012–0014

### เปลี่ยน
- `POST /events/:id/register` **ต้องมีประเภทองค์กร** (`organization_type_id` หรือ `organization_type_other`) มิฉะนั้น 400 `ORGANIZATION_TYPE_REQUIRED` (`743743b`)
- `GET /participants` มี `organization_type_id`, `organization_type_other`, `checked_in_at` (`743743b`)
- แถบเมนูเต็มเริ่มที่จอ ≥1280px; เมนูเจ้าหน้าที่ไม่มีไอคอนเพื่ออยู่แถวเดียวทั้งไทย/อังกฤษ · เมนูอังกฤษ "CMS Dashboard" → "Dashboard" (`404b7e8`)
- หน้า Dashboard กว้างสุด 1600px, ตารางพอดีจอไม่ต้องเลื่อน (≥1440px), คอลัมน์ "จัดการ" ติดขอบขวาเมื่อจอแคบ (`c45a916`)
- ข้อความ error หน้า login แสดงตามภาษาที่เลือก (`404b7e8`)
- ช่องประเภทองค์กรในหน้าลงทะเบียนเป็นรายการเลื่อนลง (dropdown) หน้าตาเหมือนช่องอื่นในฟอร์ม แทนการ์ดตัวเลือก · กรอกไม่ครบแล้วเคอร์เซอร์กลับไปที่ช่องนั้น (`ca12b58`)

### แก้ไข
- ปุ่มลอย "พักพื้นหลัง" ทับข้อความใน Footer ที่จอ ≥1024px (qa Q-6) (`404b7e8`)
- รูป QR ที่บันทึกจากแดชบอร์ดเยื้องไปทางขวาและตัดชื่อยาว (การ์ดมีระยะขอบอัตโนมัติ ซึ่งถูกคัดลอกลงในรูป) — การ์ดเต็มความกว้างและตัดระยะขอบตอนบันทึก (`c45a916`)
- บันทึกตั๋วจากหน้าลงทะเบียน/หน้าตั๋วในโทนสว่างได้พื้นดำตัวอักษรเข้ม อ่านไม่ออก — ใช้พื้นตามธีมที่เปิดอยู่ (`404b7e8`, `ca12b58`)
- มือถือ: หัวหน้าลงทะเบียน ปุ่ม "ค้นหาตั๋วของฉัน" ถูกบีบเป็น 4 บรรทัด และปุ่ม TH/EN ลอยทับช่องกรอกชื่อ — จัดเป็น 2 แถวในการ์ด (`404b7e8`)
- ไอคอนเรียงคอลัมน์ในตาราง Dashboard ถูกสร้างใหม่ทุกครั้งที่ render (lint error ลดจาก 11 เหลือ 7) (`c45a916`)

### Deploy
- restart server หนึ่งครั้งหลังอัปเดต: `initTable()` สร้างตาราง `organization_types` + คอลัมน์ใน `participants` และใส่ 6 ประเภทเริ่มต้น · ผู้ลงทะเบียนเดิมเป็น "ไม่ระบุ"

## 2026-09-23

### เพิ่ม
- **ติดตั้ง:** `scripts/setup.sh` (สร้าง `.env` พร้อมรหัสสุ่ม, เปิด DB, `npm ci`), `server/scripts/create-admin.js` (สร้าง/รีเซ็ตบัญชีเจ้าหน้าที่), `client/.env.example`
- **เอกสาร:** `docs/HANDOFF.md`, `runbook.md`, `configuration.md`, `user_manual.md`, `qa.md`, `decision.md`, ADR 0007–0011
- **Footer และ `/privacy`** — ข้อมูลผู้จัด/ติดต่อ/แผนที่/นโยบาย PDPA แก้ได้จากตั้งค่าระบบ; แถบติดต่อผู้ให้บริการแพลตฟอร์ม (`b2b0917`)
- **หน้าแรก Event Experience** — ฉาก 3D, พื้นหลังเคลื่อนไหว, ตัวอักษรเล็กอ่านง่ายขึ้น (`6f3f823`)
- **ของรางวัล:** ลำดับการสุ่ม ↑↓, ปุ่มเพิ่มรางวัล, นำเข้า/ส่งออก/ไฟล์ตัวอย่าง Excel (`72a2052`)
- **กำหนดการ:** ไฟล์ตัวอย่าง Excel, เลขลำดับบนการ์ด, การ์ดใหม่อยู่บนสุด; แท็บตั้งค่าอยู่ใน URL (`ea10f65`)
- **Dashboard:** นำเข้ารายชื่อผู้เข้าร่วมจาก Excel/CSV พร้อม preview (`c7c483f`)
- **จอ LED:** URL แยกต่อหน้าจอ, หน้าประกาศผู้โชคดีพร้อมรูปคน/รูปรางวัล (`6a283ce`)
- **Scanner:** สแกน QR ด้วยกล้องของเครื่อง (`8d1ac7a`)
- **CI:** GitHub Actions (`a9e7b28`)

### แก้ไข
- จอ LED/Scanner ตัวเลขเป็น 0 ตอนเปิดหน้า และจอ Overview พังเมื่อมีคนเช็คอิน — รูปแบบสถิติไม่ตรงกันระหว่าง REST กับ socket (`db0df18`, `6a283ce`)
- จอ LED เด้งไปหน้า Welcome ทุกครั้งที่มีคนเช็คอิน (`6a283ce`)
- หน้า Lucky ล้นขอบจอเมื่อแสดงผู้โชคดี (`6a283ce`)
- หน้า `/lucky-draw` ไม่แสดงชื่อผู้ชนะ (server ส่ง `name` แต่หน้าอ่าน `fullname`) (`6a283ce`)
- รีเฟรชหน้าตั้งค่าแล้วเด้งกลับแท็บแรก (`ea10f65`)
- นำไฟล์ส่งออกกำหนดการกลับเข้ามาแล้วรูปวิทยากรเสีย (`ea10f65`)
- รางวัลใหม่แทรกเป็นลำดับแรกแทนที่จะต่อท้าย (`72a2052`)
- `database/schema.sql` ขาดคอลัมน์ `profile_picture`, `attendee_type`; `seed.sql` พังบน DB ใหม่ (แถวที่สลับเบอร์โทรกับวันที่) — ติดตั้งเครื่องใหม่ได้แล้ว
- ลิงก์เอกสารแบบ `file:///Users/...` ใช้ไม่ได้บนเครื่องอื่น → ลิงก์ relative

### ความปลอดภัย
- ลบรหัสผ่าน DB และ JWT secret สำรองที่ฝังในโค้ด; ต้องตั้งผ่าน env (`2dd0bed`, ADR-0009)
- ลิงก์แผนที่รับเฉพาะ `http(s)://`
- คอมเมนต์ใน `seed.sql` ที่อ้างรหัสผ่าน `password123` ไม่เป็นความจริง — แก้ข้อความและให้ใช้ `create-admin.js`

### ปัญหาที่ยังค้าง
ดู [docs/qa.md §3](docs/qa.md#3-ปัญหาที่ทราบ-known-issues) — สำคัญ: `POST /lucky-draw/spin` ไม่ต้อง login, เทสต์ server 9 ข้อล้าสมัย (CI แดง)

## 2026-09-23 (ก่อนหน้า)
- Initial commit ขึ้น GitHub, สร้าง branch `Dev` (`2dd0bed`)
