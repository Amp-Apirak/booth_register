# Changelog

รูปแบบ: หัวข้อตามวันที่ (ค.ศ.) ใหม่อยู่บน · อ้างอิง commit บน branch `Dev` · เหตุผลเชิงออกแบบอยู่ใน [docs/decision.md](docs/decision.md)

## 2026-09-23

### เพิ่ม
- **ติดตั้ง:** `scripts/setup.sh` (สร้าง `.env` พร้อมรหัสสุ่ม, เปิด DB, `npm ci`), `server/scripts/create-admin.js` (สร้าง/รีเซ็ตบัญชีเจ้าหน้าที่), `client/.env.example`
- **เอกสาร:** `docs/HANDOFF.md`, `runbook.md`, `configuration.md`, `user_manual.md`, `qa.md`, `decision.md`, ADR 0007–0011
- **Footer และ `/privacy`** — ข้อมูลผู้จัด/ติดต่อ/แผนที่/นโยบาย PDPA แก้ได้จากตั้งค่าระบบ; แถบติดต่อผู้ให้บริการแพลตฟอร์ม (`b2b0917`)
- **หน้าแรก Event Experience** — ฉาก 3D, พื้นหลังเคลื่อนไหว, ตัวอักษรเล็กอ่านง่ายขึ้น (`6f3f823`)
- **ของรางวัล:** ลำดับการสุ่ม ↑↓, ปุ่มเพิ่มรางวัล, นำเข้า/ส่งออก/ไฟล์ตัวอย่าง Excel (`72a2052`)
- **กำหนดการ:** ไฟล์ตัวอย่าง Excel, เลขลำดับบนการ์ด, การ์ดใหม่อยู่บนสุด; แท็บตั้งค่าอยู่ใน URL (`ea10f65`)
- **Dashboard:** นำเข้ารายชื่อผู้เข้าร่วมจาก Excel/CSV พร้อม preview (`c7c483f`)
- **จอ LED:** URL แยกต่อหน้าจอ, หน้าประกาศผู้โชคดีพร้อมรูปคน/รูปรางวัล (`6a283ce`)
- **Scanner:** สแกน QR ด้วยกล้องของเครื่อง (`8d1ac7a`)
- **CI:** GitHub Actions (`a9e7b28`)

### แก้ไข
- จอ LED/Scanner ตัวเลขเป็น 0 ตอนเปิดหน้า และจอ Overview พังเมื่อมีคนเช็คอิน — รูปแบบสถิติไม่ตรงกันระหว่าง REST กับ socket (`db0df18`, `6a283ce`)
- จอ LED เด้งไปหน้า Welcome ทุกครั้งที่มีคนเช็คอิน (`6a283ce`)
- หน้า Lucky ล้นขอบจอเมื่อแสดงผู้โชคดี (`6a283ce`)
- หน้า `/lucky-draw` ไม่แสดงชื่อผู้ชนะ (server ส่ง `name` แต่หน้าอ่าน `fullname`) (`6a283ce`)
- รีเฟรชหน้าตั้งค่าแล้วเด้งกลับแท็บแรก (`ea10f65`)
- นำไฟล์ส่งออกกำหนดการกลับเข้ามาแล้วรูปวิทยากรเสีย (`ea10f65`)
- รางวัลใหม่แทรกเป็นลำดับแรกแทนที่จะต่อท้าย (`72a2052`)
- `database/schema.sql` ขาดคอลัมน์ `profile_picture`, `attendee_type`; `seed.sql` พังบน DB ใหม่ (แถวที่สลับเบอร์โทรกับวันที่) — ติดตั้งเครื่องใหม่ได้แล้ว
- ลิงก์เอกสารแบบ `file:///Users/...` ใช้ไม่ได้บนเครื่องอื่น → ลิงก์ relative

### ความปลอดภัย
- ลบรหัสผ่าน DB และ JWT secret สำรองที่ฝังในโค้ด; ต้องตั้งผ่าน env (`2dd0bed`, ADR-0009)
- ลิงก์แผนที่รับเฉพาะ `http(s)://`
- คอมเมนต์ใน `seed.sql` ที่อ้างรหัสผ่าน `password123` ไม่เป็นความจริง — แก้ข้อความและให้ใช้ `create-admin.js`

### ปัญหาที่ยังค้าง
ดู [docs/qa.md §3](docs/qa.md#3-ปัญหาที่ทราบ-known-issues) — สำคัญ: `POST /lucky-draw/spin` ไม่ต้อง login, เทสต์ server 9 ข้อล้าสมัย (CI แดง)

## 2026-09-23 (ก่อนหน้า)
- Initial commit ขึ้น GitHub, สร้าง branch `Dev` (`2dd0bed`)
