# Copy to client/.env.local
# URL of the Express API + Socket.io server as seen from the browser.
# On an event LAN use the server's IP, e.g. http://192.168.1.10:3005
NEXT_PUBLIC_API_URL=http://localhost:3005
